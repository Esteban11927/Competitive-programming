#include<iostream>
using namespace std;
int main(){
    int t, a[3];
    cin >> t;
    while(t--){
        cin >> a[0] >> a[1] >> a[2];
        if(a[0] > a[2]) swap(a[0], a[2]);
        if(a[1] > a[2]) swap(a[1], a[2]);
        if(a[0] > a[1]) swap(a[0], a[1]);
        int x = a[]
    }
    return 0;
}