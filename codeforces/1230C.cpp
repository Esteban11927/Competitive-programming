#include<iostream>
#include<vector>
#include<map>
using namespace std;
int main(){
    int n, m;
    cin >> n >> m;
    vector<int> gr[n+1];
    for(int i = 0; i < n; i++){
        cin >> a >> b;
    }
}