#include<iostream>
using namespace std;
int main(){
    int n, r;
    cin >> n >> r;
    int arr[n];
    for(int i = 0; i < n; i++) cin >> arr[i];
    int last = -1;
    while(last < n){

    }
    return 0;
}